name: store_flutter
description: A new Flutter project.

version: 0.0.0+1

author: John Zhou<zhougonglai@dingtalk.com>

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  shared_preferences: ^0.5.2
  fluro: ^1.5.1
  http: 0.12.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
